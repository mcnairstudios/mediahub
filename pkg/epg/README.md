# epg

EPG (Electronic Program Guide) data types and store interfaces.

Defines `Source` (an XMLTV feed) and `Program` (a scheduled broadcast). Store interfaces (`SourceStore`, `ProgramStore`) are implemented by persistence packages elsewhere in the tree.

Zero external dependencies.
